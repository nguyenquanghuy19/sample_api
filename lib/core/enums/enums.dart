enum Flavor {
  develop,
  production,
  staging,
}

enum BuildMode {
  debug,
  profile,
  release,
}

enum OnOffState { on, off }

enum Level {
  error,
  warn,
  info,
  debug,
  trace,
}

enum BottomTabItemBeforeSignIn {
  home,
  course,
  setting,
}

enum BottomTabItemAfterSignIn {
  home,
  course,
  myLearning,
  setting,
}

enum FlashCardSelectedState {
  all,
  remember,
}

enum TypeToast {
  success,
  error,
  warning,
  custom,
}

enum MenuMyLearningDetail {
  information,
  roadmap,
  result,
  rank,
  quiz,
  flashCard,
  slideShow,
}
